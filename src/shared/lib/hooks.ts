// Placeholder hooks file kept for compatibility with old imports.
// Redux is not used in this project.
export const useAppDispatch = () => {
  throw new Error("Redux is not configured in this project.");
};

export const useAppSelector = () => {
  throw new Error("Redux is not configured in this project.");
};
